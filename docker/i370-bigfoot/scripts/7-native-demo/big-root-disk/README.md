## Bare-bones root file system.
 
Copy this to your root disk image.  Be sure to create the required
special files, with the `makedev.sh` script.

----------------------------------------------------------
